<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.viewgroups">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.example.viewgroups.Activities.GridViewActivity" android:label="GridView"></activity>
        <activity android:name="com.example.viewgroups.Activities.TableLayoutActivity" android:label="TableLayout"/>
        <activity android:name="com.example.viewgroups.Activities.WebViewActivity" android:label="WebView"/>
        <activity
            android:name="com.example.viewgroups.Activities.ListViewActivity"
            android:label="ListView">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>